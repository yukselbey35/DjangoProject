Create a virtual enviroment.  =python -m venm env 
Activate the env visual enviroment.= env\scripts\activate
Now we are in the visual enviroment
Checks packages we installed via pip command in a requirements format =pip freeze 
Install python django packages =pip Install django
upgrading c:\users\yak\desktop\github\djangoproject\env\scripts\python.exe -m pip install --upgrade pip' command.
If you need django commands =django-admin --help
I would rather change the directory name as container  DjangoProject_Container
Run the server =python manage.py runserver
Deactivate virtual enviroment and clsoe VSC= deactivate 